# Landing page environment variables
# Copy to .env.local for local development

# Merchant dashboard URL (used as Moyasar callback base URL)
NEXT_PUBLIC_MERCHANT_URL=https://merchant.pointly.sa

# API base URL (used by the signup modal to call initiate-signup)
NEXT_PUBLIC_API_URL=https://api.pointly.sa
